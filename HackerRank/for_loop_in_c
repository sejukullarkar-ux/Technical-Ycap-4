#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    int a, b;
    scanf("%d\n%d", &a, &b);
    for(int i=a;i<=b;i++){
    char *numbers[] = {"", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};
    if(i>=1 && i<=9){
  	     printf("%s",numbers[i]);
    }else if(i%2==0){
        printf("even");        
    }else{
       printf("odd");  
    }
    printf("\n");
}return 0;
}
